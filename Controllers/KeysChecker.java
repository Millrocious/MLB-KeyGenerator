package Controllers;

import java.util.HashMap;
import java.util.Map;

public class KeysChecker {
    private HashMap<Integer, Boolean> basePairs;

    public KeysChecker(HashMap<Integer, Boolean> basePairs) {
        this.basePairs = basePairs;
    }

//    public void isKeysExist() {
//        HashMap<Integer, Boolean> isExists = new HashMap<>();
//        for (Map.Entry<Integer, Boolean> entry : basePairs.entrySet()) {
//            for () {
//                isExists.put(entry.getKey().probablyKeys.get(0)[0], kG.getKey().checkIsKeysExist());
//            }
//        }
//        System.out.println(isExists);
//    }
}
